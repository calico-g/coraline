
<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "name" %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.radio_button(:item_type, "number") %>
    <%= f.label(:item_number, "number") %>
    <%= f.radio_button(:item_type, "scale") %>
    <%= f.label(:item_scale, "scale") %>
    <%= f.radio_button(:item_type, "boolean") %>
    <%= f.label(:item_boolean, "boolean") %>
  </div>
  <div class="actions">
    <%= f.submit "add new item", class: "btn" %>
  </div>
<% end %>
